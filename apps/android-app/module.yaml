product: android/app

apply:
  - ../../jetbrains.module-template.yaml
  - ../../android.module-template.yaml

dependencies:
  - ../shared

settings:
  junit: junit-4
  android:
    namespace: com.cmpisfun.aldikitta